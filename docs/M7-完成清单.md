# M7 · 报告中心完成清单

## ✅ 已完成

### 数据模型
- reports 表（type/target/title/period/content/summary/pdf_key）

### 后端
- 3 类报告生成：班级周报 / 学生档案 / 学校综合报告
- 数据采集：自动汇总学生/班级/学校多维数据
- AI 综合建议：通义千问基于数据生成 200~300 字建议（mock 时也有兜底）
- PDF 渲染：reportlab + 中文字体 STSong-Light，支持 H1/H2/列表
- PDF 自动保存到本地存储 + 在线下载

### REST 接口（5 个）
- GET  /api/reports
- GET  /api/reports/{id}
- GET  /api/reports/{id}/pdf
- POST /api/reports/class/{class_id}
- POST /api/reports/student/{student_id}
- POST /api/reports/school

### 前端
- 报告中心页：列表 + 类型筛选 + 生成对话框（班级/学生/学校）+ 在线查看（Markdown 渲染）+ PDF 下载

### 测试
- 37 个后端测试全过（新增 4 个）
