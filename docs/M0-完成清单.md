# M0 · 基础设施完成清单

本里程碑的目标：**搭好仓库骨架，让前端、后端、AI 服务三条腿都能独立启动并互相握手**。

## ✅ 已完成

### 1. 仓库结构
- 完整目录骨架（backend / frontend / ai_service / scripts / docker / docs）
- `.gitignore`、`.env.example`、`README.md`

### 2. Flask 后端
- 应用工厂模式 (`create_app`)
- 扩展集中管理（SQLAlchemy / JWT / Marshmallow / SocketIO / CORS / Migrate）
- 多环境配置类（Development / Production / Testing）
- 健康检查接口：`GET /api/health`、`GET /api/health/deep`（探测 MySQL / Redis / MinIO / AI 服务）
- 统一响应格式工具
- 全局错误处理（404 / 405 / 500）
- loguru 日志（按天滚动 + 控制台）
- pytest 单元测试（健康检查通过）

### 3. AI 推理服务（FastAPI）
- 服务骨架 + 健康检查 + 设备信息接口
- 流水线目录结构（M2 起填充）
- CORS 配置

### 4. Vue 3 前端
- Vite + TypeScript + Vue 3.4
- Element Plus 中文化 + 图标自动注册
- Pinia / Vue Router
- 自动按需引入（unplugin-auto-import + unplugin-vue-components）
- 路径别名 `@/*`
- Axios 封装（统一拦截、错误提示、Token 注入）
- 系统接口封装 (`api/system.ts`)
- 登录页（带后端联通检测）
- 首页（5 项依赖服务实时状态卡片 + 9 阶段路线图）
- 404 页

### 5. Docker 编排
- `docker-compose.yml`：MySQL 8 / Redis 7 / MinIO / 后端 / 前端 / AI
- `docker/Dockerfile.backend`、`Dockerfile.ai`、`Dockerfile.frontend`、`nginx.conf`
- 健康检查、依赖关系、卷管理、网络划分

### 6. 文档
- README（产品介绍、技术栈、快速开始、里程碑）
- 架构设计.md
- 伦理设计说明.md
- M0-完成清单.md（本文档）

## 🧪 验证方式

### 本地（已激活 conda 环境 `kt_cursor`）

```bash
# 1. 安装后端依赖
cd backend && pip install -r requirements.txt

# 2. 启动 MySQL/Redis/MinIO（任选一种）
# 方式 A：用 Docker
docker compose up -d mysql redis minio
# 方式 B：本地已有，自行启动

# 3. 启动后端
cd backend && python run.py
# 访问 http://localhost:5000/api/health

# 4. 启动 AI 服务
cd ai_service && pip install -r requirements.txt && python server.py
# 访问 http://localhost:8000/health

# 5. 启动前端
cd frontend && npm install && npm run dev
# 访问 http://localhost:5173
```

### Docker 一键启动

```bash
cp .env.example .env
docker compose up -d
docker compose logs -f
```

### 后端单元测试

```bash
cd backend && pytest
```

## 📦 下一里程碑（M1 · 用户与权限）

- 数据库表设计：schools / grades / classes / students / teachers / subjects / users / roles / permissions
- Flask-Migrate 初始化 + 第一个迁移
- JWT 登录、注册、Token 刷新
- 5 级 RBAC 权限拦截器
- 学校 / 年级 / 班级 / 学生 / 教师 CRUD 接口
- 演示数据 seed 脚本（1 所学校、3 个年级、6 个班、~180 个学生、~30 个教师）
- 前端：登录联调、用户管理页、组织架构页、角色权限页
